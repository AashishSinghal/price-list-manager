export type IProduct = {
  name: string;
  description: string;
  unit: number;
  price: number;
  discount: string;
  image: string;
  business: string;
};

export type IBusiness = {
  name: string;
};
